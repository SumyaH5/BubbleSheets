
import SwiftUI
import AVKit
struct SavedView: View {
    @State var audioPlayer: AVAudioPlayer!
    var body: some View {
        Text("This is the saved view")
            
    }
}
struct SavedView_Previews: PreviewProvider {
    static var previews: some View {
        SavedView()
    }
}
